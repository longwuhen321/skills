# ============================================================
# 仓库根 .gitignore — skills 多模块仓库通用忽略规则
# ============================================================

# ---------- 通用规则（适用于仓库内所有 skill） ----------

# Python 字节码缓存
__pycache__/
*.pyc

# IDE 本地配置（不提交，各人本机习惯不同）
.vscode/
.idea/

# 自带skill，不提交
/.system

# 真实配置文件 不提交
**/config.py

# 不提交项目中的调试日志快照（<skill>/logs/，体积可能很大）
/*/logs

# 验证依赖目录，不提交
/.validation_deps