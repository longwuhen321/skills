# ============================================================
# 仓库根 .gitattributes — 统一行尾策略（LF）
# ============================================================
# 本仓库全部文本文件统一使用 LF 行尾（Windows/macOS/Linux 一致），
# 避免 git 在 Windows 上自动转换 CRLF 导致 diff 漂移。
# text=auto 让 git 自动识别文本/二进制；eol=lf 强制 checkout 保持 LF。

* text=auto eol=lf

# 明确标记文本类型（eol=lf 已由上行覆盖，此处仅显式声明常见扩展名）
*.md   text
*.py   text
*.json text
*.txt  text
*.toml text
*.html text
.gitignore text
.gitattributes text
