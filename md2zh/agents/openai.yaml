interface:
  display_name: "Markdown to Chinese"
  short_description: "Translate Markdown to Chinese"
  default_prompt: "Use $md2zh to translate the requested Markdown file to Chinese."

policy:
  allow_implicit_invocation: false
